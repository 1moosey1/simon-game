import styles from './header.scss';

module.exports = React.createClass({

    render: function() {

        return (
            <header>
                <h1> Simon Game </h1>
            </header>
        );
    }
});